# How to create a new user on OSX remotely
run mkuser
in caes file fault is active you have to go to Privacy & Security and "enable" the user to decrypt them. Just type the new user's password once.

# Remove user remotely
dscl . delete /users/USER

# In case the files still persist in /Users/NEWUSER
rm -rv /Users/NEWUSER
